---
import { getRelativeLocaleUrl } from "astro:i18n";

export const prerender = false;
console.log(Astro.preferredLocale);
console.log(Astro.request.headers.get("accept-language"));
console.log(Astro.preferredLocaleList);
console.log(Astro.currentLocale);

const url = getRelativeLocaleUrl(Astro.currentLocale, "/home");
return Astro.redirect(url);
---
