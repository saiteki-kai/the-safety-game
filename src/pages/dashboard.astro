---
export const prerender = false;

import DashboardClient from "@components/dashboard/DashboardClient";
import PageLayout from "@layouts/PageLayout.astro";

// const supabase = createClient({
//   request: Astro.request,
//   cookies: Astro.cookies,
// });

// const { data } = await supabase.auth.getClaims();

// if (!data?.claims) {
//   Astro.redirect("/login");
// }
const user = Astro.locals.user || null;
console.log("Dashboard page user:", !!user);

const team = Astro.locals.team || null;
console.log("Dashboard page team:", !!team);

// const result = Astro.getActionResult(actions.teams.createTeam);
// console.log("Dashboard page action result:", result);
---

<PageLayout>
  <DashboardClient client:load user={user} team={team} />
</PageLayout>
