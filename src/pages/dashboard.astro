---
export const prerender = false;

import DashboardClient from "@components/dashboard/DashboardClient";
import PageLayout from "@layouts/PageLayout.astro";

// TODO: replace with real team data from Supabase once available
const team: { name?: string } | null = null;
---

<PageLayout>
  <DashboardClient client:load team={team} />
</PageLayout>
