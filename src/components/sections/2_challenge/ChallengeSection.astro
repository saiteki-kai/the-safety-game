---
import Card from "@components/common/Card.astro";
import SectionLayout from "@layouts/SectionLayout.astro";
import type { ChallengeContent } from "@/i18n/locales/types";

const { challenge } = Astro.props as { challenge: ChallengeContent };
---

<SectionLayout
  id="challenge"
  title={challenge.title}
  description={challenge.description}
  sectionClass="bg-white"
>
  <div class="space-y-8">
    <div class="py-8">
      <p class="gradient-text text-center text-3xl sm:text-4xl font-semibold mb-0">{challenge.tagline}</p>
    </div>

    <Card
      class="p-6"
      label={challenge.objectiveLabel}
      icon="lightbulb"
      iconClass="text-rose-600 shrink-0"
    >
      <div class="space-y-2">
        {
          challenge.what
            .split(/(?<=[.!?])\s+/)
            .map((sentence) => {
              const isHighlight = sentence.includes("Crea prompt basati sul contesto culturale italiano");
              return (
                <p class={"text-neutral-700 leading-relaxed " + (isHighlight ? "text-rose-400 font-semibold text-lg" : "")}>{isHighlight ? <span>{sentence}</span> : sentence}</p>
              );
            })
        }
      </div>
    </Card>
    
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
      <Card
        class="p-6"
        label={challenge.challengeLabel}
        icon="star"
        iconClass="text-yellow-400 shrink-0"
      >
        <p class="text-neutral-700 leading-relaxed mb-4">
          {challenge.challengeText}
        </p>
      </Card>

      <Card
        class="p-6"
        label={challenge.participationLabel}
        icon="users"
        iconClass="text-green-600 shrink-0"
      >
        <p class="text-neutral-700 leading-relaxed">
          {challenge.participation}
        </p>
      </Card>
      
      <Card
        class="p-6"
        label={challenge.prizesLabel}
        icon="trophy"
        iconClass="text-amber-600 shrink-0"
      >
        <p class="text-neutral-700 leading-relaxed" set:html={challenge.prizesDescription}></p>
      </Card>
    </div>
    
    <Card
      class="p-6"
      label={challenge.llmLabel}
      icon="ai"
      iconClass="text-blue-600 shrink-0"
    >
      <div class="space-y-4">
        {
          challenge.introduction.map((paragraph) => (
            <p class="text-neutral-700 leading-relaxed">{paragraph.content}</p>
          ))
        }
      </div>
    </Card>
  </div>
</SectionLayout>
