---
import "@styles/globals.css";

import { ClientRouter } from "astro:transitions";
import { ScrollToTopButton } from "@components/common/ScrollToTopButton";
import { Toaster } from "@components/ui/sonner";
import WebsiteAnalytics from "@vercel/analytics/astro";
import SpeedInsights from "@vercel/speed-insights/astro";

import { type Locale, DEFAULT_LOCALE } from "@/lib/translations";

interface Props {
    title: string;
    description?: string;
    lang?: Locale;
    fullScreen?: boolean;
}

const {
    title,
    description = "The Safety Game - Hackathon organized by MIND, University of Milan Bicocca",
    lang,
    fullScreen = false,
} = Astro.props as Props;

const bodyClass = fullScreen ? "min-h-dvh h-dvh bg-black flex flex-col" : "min-h-dvh h-dvh bg-white flex flex-col";

const locale = lang ?? DEFAULT_LOCALE;
---

<html lang={locale || "it"}>
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <meta name="description" content={description} />
        <meta name="generator" content={Astro.generator} />
        <title>{title}</title>
        <ClientRouter />
    </head>
    <body class={bodyClass}>
        <slot name="navigation" />
        <Toaster client:load />
        <WebsiteAnalytics />
        <SpeedInsights />
        <main
            id="main-content"
            class={fullScreen ? "flex flex-col flex-1 min-h-dvh w-full overflow-hidden" : "flex flex-col flex-1 min-h-0 w-full overflow-auto"}
        >
            <slot />
        </main>
        <ScrollToTopButton client:idle />
        <slot name="prefooter" />
        <slot name="footer" />
    </body>
</html>
